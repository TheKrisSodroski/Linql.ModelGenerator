using Linql.ModelGenerator.Backend;
using Linql.ModelGenerator.Intermediary;
using NUnit.Framework;
using System.ComponentModel.DataAnnotations;
using System.Text.Json;
using Test.Module1;

namespace Linql.ModelGenerator.Frontend.Test
{
    public class LinqlModelGenerator_EntityFramework
    {

        //[Test]
        //public async Task Generate()
        //{
        //    LinqlModelGeneratorCSharpBackend generator = new LinqlModelGeneratorCSharpBackend(typeof(KeyAttribute).Assembly);
        //    IntermediaryModule module = generator.Generate();
        //    //this.ModuleJson = JsonSerializer.Serialize(this.Module, this.JsonOptions);
        //    //this.Generator = new LinqlModelGeneratorCSharpFrontend(this.ModuleJson);

        //}
    }
}